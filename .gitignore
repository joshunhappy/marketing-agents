# Environment
.env
.env.*
!.env.example

# Per-brand secrets (each brand has its own .env)
brands/*/.env

# Active brand pointer is per-developer, not committed
.active-brand

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Reports (agent outputs — store locally, not in git)
reports/*
!reports/.gitkeep

# Archived data from `marketing-agents reset` (kept for human review only)
_archive/

# Logs
logs/
*.log

# macOS
.DS_Store

# Claude Code — local-only settings
.claude/settings.local.json
